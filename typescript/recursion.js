/*
Recursion codesmith
Mike 
global execution context / global memory (store variables and functions)

function multiplyBy2(num) { retrun 2*num; } 

const output = multiplyBy2(3);

local excution context / local memory (store variables and functions)

thread ofe xcution -> java single tread
call stack

initial value, breaking condition, update expresion


stack frame replacement = te tengo loq ue encesitas mami, todo en return
super optimizadda

cuando no lllamo la recursion
como resultaod final, no abro execution contex (se abre )
simplmeente modifico el que ya esta


stack overflow error si se pasa en el llamdao de stack
mutual recursion -> no se llama a si mismo
memoaisesion
*/